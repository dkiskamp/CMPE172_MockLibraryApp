import React, {Component} from 'react';

export default class Book extends React.Component {
    constructor(props) {
      super(props);
    }
  
    render() {
      return (
          <h1>book</h1>
          //code here for the view of library list
      );
    }
  }